;; This is fun because I have no idea how to do it!
;; Lets start maybe with making some threejs models with clojurescript.
(ns bestguy.core)

(defn foo
  "I don't do a whole lot."
  [x]
  (println x "Hello, World!"))
